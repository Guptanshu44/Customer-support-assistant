# ============================================================
# CareBot — Python Dependencies
# ============================================================

# LLM Providers (Groq is recommended & free / Claude optional)
groq>=0.9.0
anthropic>=0.40.0

# Environment
python-dotenv>=1.0.0

# Knowledge Base (FAISS + Embeddings)
# Note: sentence-transformers installs torch automatically (lighter build)
faiss-cpu>=1.7.4
sentence-transformers>=2.7.0
numpy>=1.26.0

# Streamlit Cloud deployment
streamlit>=1.30.0

# Local AI / NLP offline fallback (optional — comment out if not needed)
# Uncomment below ONLY for local Flask server use or offline HuggingFace mode
# torch>=2.0.0
# transformers>=4.40.0
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
scipy>=1.11.0
# eventlet>=0.35.0
